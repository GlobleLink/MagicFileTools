import React from 'react';
import { createRoot } from 'react-dom/client';
const root = createRoot(document.getElementById('root'));
root.render(React.createElement('div', { style: { textAlign: 'center', paddingTop: '60px', fontSize: '20px' } }, '✅ Real Text Processor Bundle Loaded'));
